# Workspace Customization Rules

- **Micro-commits**: Always make micro-commits with less than 100 insertions per commit (mandatory).
- **PRs and Branches**: Make new branches and pull requests for new feature work.
- **Topology of branches**: Never use fast-forward merges; always enforce branch topology using `--no-ff`.
